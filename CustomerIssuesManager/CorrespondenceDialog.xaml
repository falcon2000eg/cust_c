<Window x:Class="CustomerIssuesManager.CorrespondenceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إضافة مراسلة جديدة" 
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="إضافة مراسلة جديدة" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
        
        <!-- Next Yearly Number -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <Label Content="الرقم السنوي التالي:" FontWeight="Bold" Foreground="#2980b9"/>
            <TextBlock x:Name="NextYearlyNumberTextBlock" 
                       Text="جاري التحميل..." 
                       FontSize="14" 
                       FontWeight="Bold" 
                       Foreground="#27ae60"
                       Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Sender -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <Label Content="المرسل:" FontWeight="Bold"/>
            <TextBox x:Name="SenderTextBox" 
                     Text=""
                     Height="30"
                     FontSize="14"/>
        </StackPanel>
        
        <!-- Message Content -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <Label Content="محتوى المراسلة:" FontWeight="Bold"/>
            <TextBox x:Name="MessageContentTextBox" 
                     Text=""
                     Height="200"
                     FontSize="14"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button Content="حفظ" 
                    Width="100" 
                    Height="35"
                    Margin="0,0,10,0"
                    Background="#27ae60"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="SaveButton_Click"/>
            <Button Content="إلغاء" 
                    Width="100" 
                    Height="35"
                    Background="#95a5a6"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 